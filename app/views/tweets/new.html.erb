<h3 class = "text-center">新規投稿</h3>

<div class = "row">
  <div class = "offset-sm-1 col-sm-10">
    
    <%= form_with(model: @tweet, local: true ) do |f| %>
    
    <%= render("tweet_form", tweet: @tweet,f: f)%>

    <%= f.submit "投稿", class: "btn btn-primary btn-block"%>
    <% end %>
  </div>
</div>
